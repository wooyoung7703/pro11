#############################################
# Local Private Secrets (DO NOT COMMIT)
#############################################
# Copy this file to `.env.private` and fill in real credentials locally.
# Ensure `.env.private` is in .gitignore (it already should be).
# The app should load `.env` first, then merge `.env.private` if present.

# Exchange (Binance) real API keys (EXAMPLE KEYS SHOWN AS EMPTY)
BINANCE_API_KEY=
BINANCE_API_SECRET=

# If you need to enable real trading locally (NOT RECOMMENDED), you must:
# 1) Unset testnet and explicitly enable trading here, and
# 2) Allow safety override — only if you absolutely know what you're doing.
# BINANCE_TESTNET=false
# EXCHANGE_TRADING_ENABLED=true
# SAFETY_ALLOW_REAL_TRADING=1

# App API key override (leave empty to auto-generate)
API_KEY=
