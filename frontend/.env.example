# 프론트엔드 환경 예시 (Vite)
# 백엔드 프록시 대상 설정
# docker-compose 로 서비스명이 app 인 경우
#VITE_BACKEND_URL=http://app:8000

# 로컬 머신에서 백엔드를 직접 띄운 경우
#VITE_BACKEND_URL=http://localhost:8000

# 개발 편의: 목 데이터 사용 (0 또는 1). 기본 0
VITE_USE_MOCKS=0

# 백엔드 SSE/WS 베이스 URL (미설정 시 현 origin 기준으로 자동)
# 예: ws://localhost:8000
#VITE_WS_BASE=ws://localhost:8000

# 백엔드 추론 자동 루프 간격과 비교용 값(초). 
# Trading 화면에서 백엔드 interval과의 불일치를 알려줍니다.
#VITE_INFERENCE_AUTO_LOOP_INTERVAL=15

# --- 실시간 SSE 임계값 및 심각도 설정 ---
# Vite는 VITE_ 접두어인 변수만 노출합니다. 모든 값은 문자열로 주입되며 코드에서 숫자로 파싱합니다.

# 시그널 SSE 신선도 임계값(ms)
# server_time age <= 임계값 이면 fresh 로 간주
VITE_SIG_FRESH_MS=20000

# 리스크 SSE 신선도 임계값(ms)
VITE_RISK_FRESH_MS=15000

# 심각 지연 배수
# age > fresh_ms * VITE_SEVERE_LAG_MULT → 심각(danger), fresh_ms 초과 → 경고(warning)
VITE_SEVERE_LAG_MULT=3

# (선택) 개발 편의용 API 키 자동 주입
# 로컬 개발 환경에서 App 좌측 상단 입력 없이 사용할 기본 키를 설정할 수 있습니다.
# VITE_DEV_API_KEY=dev-key

# Backfill Runs SSE 신선도 임계값(ms)
# Job Center의 Backfill 카드 상단 신선도 배지 색상/문구에 사용됩니다.
# 설정하지 않으면 기본값은 fresh=10000, stale=30000 입니다.
# VITE_RUNS_FRESH_MS=10000
# VITE_RUNS_STALE_MS=30000

# --- AdminPanel 기본값/간격 설정 ---
# Bootstrap 임계치 기본값(프론트 폼 초기값으로 사용)
# VITE_BOOTSTRAP_MIN_AUC=0.65
# VITE_BOOTSTRAP_MAX_ECE=0.05

# Risk 자동 새로고침 간격(ms)
# Admin → Risk State 패널의 auto 라벨 문구와 동작에 반영됩니다.
# VITE_RISK_AUTO_INTERVAL_MS=15000

# Feature Backfill Runs 폴링 간격(ms)
# Admin → Feature Backfill Runs 패널에서 auto가 켜져 있을 때 사용됩니다.
# VITE_BACKFILL_POLL_MS=10000

# --- 감성(Sentiment) UI 기본값/토글 ---
# 감성 윈도우(분단위, 콤마 구분). 백엔드 기본(5m,15m,60m)과 일치시키는 것을 권장합니다.
# VITE_SENTIMENT_WINDOWS=5m,15m,60m

# 감성 대상 기본 심볼 (미설정 시 서버 기본값 사용)
# VITE_SENTIMENT_SYMBOL=XRPUSDT

# 감성 SSE 스트림 사용 여부 (백엔드 SENTIMENT_ENABLE_SSE=true 필요)
# VITE_SENTIMENT_SSE_ENABLED=1

# 감성 히스토리 기본 step (예: 1m/5m)
# VITE_SENTIMENT_STEP_DEFAULT=1m

# Admin 학습 트리거 시 감성 ablation 토글 기본값 (UI 프리셋)
# VITE_TRAINING_ABLATE_SENTIMENT_DEFAULT=0

# --- Drift 스캔 기본값(프론트 초기값) ---
# Feature Drift 화면의 초기 윈도우/임계값 추천값입니다. 미설정 시 window=200, |Z|=3.0 사용.
# 운영 환경에서 더 민감/덜 민감하게 조정하려면 아래 값을 설정하세요.
# VITE_DRIFT_WINDOW_DEFAULT=200
# VITE_DRIFT_Z_THRESHOLD_DEFAULT=3.0

# Drift 자동 스캔 기본 토글/주기(초)
# 미설정 시 auto=on, interval=60s 로 동작합니다.
# VITE_DRIFT_AUTO_DEFAULT=1   # 1/true: 자동스캔 ON, 0/false: OFF
# VITE_DRIFT_AUTO_INTERVAL_SEC=60
